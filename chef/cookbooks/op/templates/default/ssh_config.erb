# Managed by Chef
<% @hosts.each do |_, cfg| -%>
Host <%= cfg['Host'].join(' ') %>
  <% cfg.reject { |k, _| k == 'Host' }.sort { |(k1, _), (k2, _)| k1 <=> k2 }.each do |key, value| -%>
    <%= key %> <%= value.respond_to?(:each) ? value.join(' ') : value %>
  <% end %>
<% end %>
