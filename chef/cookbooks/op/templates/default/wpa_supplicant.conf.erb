# maintained by Chef
ctrl_interface=DIR=/var/run/wpa_supplicant GROUP=netdev
country=FR
update_config=1
<% @networks.sort.each do |ssid, psk| -%>
network={
    ssid="<%= ssid %>"
    psk=<%= psk %>
}
<% end -%>
