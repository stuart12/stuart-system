# Managed by Chef
if [ -x <%= @bin %> ]
then
  PATH=<%= @bin %>:$PATH
  if [ -x <%= @java %> ]
  then
    JAVA_HOME=<%= @java %>; export JAVA_HOME
  fi
  GRADLE_USER_HOME=$HOME/.cache/gradle; export GRADLE_USER_HOME
fi
